{
  "name": "staxly",
  "version": "1.0.0",
  "description": "",
  "author": "Philip Schatz <phil@cnx.org>",
  "license": "ISC",
  "repository": "https://github.com/openstax/staxly.git",
  "keywords": [
    "probot",
    "github",
    "probot-app"
  ],
  "scripts": {
    "dev": "nodemon",
    "lint": "standard --fix",
    "start": "probot run ./index.js",
    "test": "jest && standard --fix",
    "test:debug": "NODE_ENV=development node --inspect-brk $(npm bin)/jest -i --updateSnapshot",
    "test:watch": "jest --watch --notify --notifyMode=change --coverage"
  },
  "dependencies": {
    "@slack/client": "^4.0.0",
    "probot": "^9.9.1",
    "smee-client": "^1.0.1"
  },
  "devDependencies": {
    "jest": "^22.4.3",
    "nodemon": "^1.14.11",
    "standard": "^11.0.0",
    "weak": "^1.0.1"
  },
  "engines": {
    "node": ">= 10.13.0",
    "npm": ">= 6.11.3"
  },
  "standard": {
    "env": [
      "jest"
    ]
  },
  "jest": {
    "testEnvironment": "node"
  },
  "nodemonConfig": {
    "exec": "npm start",
    "watch": [
      ".env",
      "."
    ]
  }
}
